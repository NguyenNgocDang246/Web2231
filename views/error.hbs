<style>
    /* Đặt lại các thuộc tính mặc định */
    * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }

    body {
        font-family: 'Arial', sans-serif;
        background-color: #f9f9f9; /* Màu nền nhẹ hơn */
        color: #333; /* Màu chữ chính */
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh; /* Đảm bảo toàn màn hình */
        padding: 20px; /* Đệm để không bị quá sát cạnh màn hình nhỏ */
    }

    .error-container {
        text-align: center;
        background: #ffffff;
        border: 2px solid #000000;
        border-radius: 10px;
        padding: 30px 50px;
        box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2); /* Đổ bóng rõ hơn */
        max-width: 500px; /* Giới hạn chiều rộng */
        width: 100%;
    }

    .error-code {
        font-size: 100px;
        font-weight: bold;
        color: #007BFF; /* Màu xanh nổi bật */
        margin-bottom: 10px;
    }

    .error-message {
        font-size: 18px;
        color: #555555; /* Màu trung tính hơn */
        margin-bottom: 20px;
    }

    .home-link {
        display: inline-block;
        text-decoration: none;
        color: #ffffff;
        background-color: #007BFF; /* Nút xanh dương */
        padding: 10px 20px;
        font-weight: bold;
        border-radius: 5px;
        transition: background-color 0.3s ease;
    }

    .home-link:hover {
        background-color: #0056b3; /* Màu xanh đậm hơn khi hover */
    }
</style>

<div class="error-container">
    <div class="error-code">{{errorCode}}</div>
    <div class="error-message">{{errorMessage}}</div>
    <a href="/product/all" class="home-link">Go Back Home</a>
</div>
